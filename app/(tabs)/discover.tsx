import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Page = () => {
  return (
    <View style={{ backgroundColor: "#FFF", flex: 1 }}>
      <Text style={{  }}>tab discover</Text>
    </View>
  );
};
export default Page;

import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Page = () => {
  return (
    <SafeAreaView>
      <View style={{ backgroundColor: "#FFF" }}>
        <Text style={{}}>tab me</Text>
      </View>
    </SafeAreaView>
  );
};
export default Page;
